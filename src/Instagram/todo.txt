FIX:	Unfollowing someone that isn't tracked, starts tracking them with { isFollowing:false }
FIX:	downloading from Pop-up window doesn't add download to users dl{}
FIX:	https://www.instagram.com/carlie.nicole/ red & black bikini photo has 2 owners and both get marked with the download.
FIX:	the public/private indicators are getting appened to the vsco links instead of the following button.

IDEA:	For people with DL, split out by last year they produced so we can pick a good prune cut-off # of years.
IDEA:	use lastUpload or lastGood to seed lastVisit where missing
IDEA:	have tab text based on new images, not total images when available.
IDEA:	Button to open 10 tabs of lastViewed/stale people (set a flag so tab knows it was opened as such), if a tab finds no new images, set window timer for 20 seconds to load the next stale, or close window/tab. and show a cancel-timer button.
FIX:	Collapsed view should have width of 0 so it doesn't obstruct images under it for normal scrolling
SCRUB:	username, id, fullname, score

CHECK:	If we prune someone, do unflush updates on them bring them back?

Tests:
	downloading from (location), (saved-posts), or (tagged) page
		- if user is tracked
			- incrments dl
		- if user is NOT tracked
			- does not increment dl.
			- does NOT update view date
	on user page
		- downloads
			- updates DL
			- if no viewState, sets it
		- if isTracked 
			- updates viewDate
		- if not tracked 
			- does not updat viewDate
			- then downloads, increments DL and sets viewDate

	scrolling through users we are following
		- tracks all followed users with .isFollowing = true
			- ** can creat followed Users with no DL nor viewDate

	viewDate / stale
		- should include all tracked users missing viewDate
		- should include users with viewDate if it is past the refresh time.
		- users missing viewDate should come first.
		- visiting a tracked person's page, updates viewDate

		ViewDate Groups:
		if( viewDate !== undefined )
			return isStale ? "stale"		// LIST 
				: "fresh";
		else if(dl !== undefined)
			return "dl wants matching viewDate"; // LIST
		else if( isFollowing )
			return "vitit-new-followees"	// LIST
		else
			return "remove-from-repo";

	DL
		- only set DL if on user's page or isTracked or isFollowing
		- downloading on users page, set viewState and DL

	Pruning
		- only prune/notify people with viewDate
		Scaned Lists / Links
			=> "Prune List-unknown" => no-dl => NOTIFY
			=> "Prune List-following" => dl & none in 4 years, following => NOTIFY
			=> "Prune List-prune"     => dl & none in 4 years, not following => PRUNE

		- on (.isTracked) USER PAGE
			- if no downloads => show and offer to PRUNE
			- if following but DL are older than 4 years => show and offer to unfollow / prune		

		To get off of "Prune List-Unknown"?
			Option - Download something
			Option - unfollow / prune

	If( DL ){
		IF (nothing for 4 years)
			"PRUNE"
		else
			"PRODUCING/CURRNET"
	} else IF ( isFollowing )
		"no-dl followee"
	else
		"missing DL";

672zcU

Remove extra fields saved on localStorage.user.

Wrap UserRepo so nothing interacts with UserRepo directly.


Clean up Groups


'lastView:D_L:fresh:          :      ', 2037	GOOD
'lastView:D_L:stale:          :      ',  680	GOOD

'lastView:   :fresh:          :      ', 1624	OK - will have long refresh time
'lastView:   :stale:          :      ', 2804	OK - already passed refresh time

'        :D_L:stale:          :      ', 285		This can happen if we download from a location or from a users tagged page.

'        :   :stale:          :      ', 1242	Review then prune

