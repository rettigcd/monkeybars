Add a cmd function called table()
- it generetes a UI table that contains columns for all of the employee properties that we save.
- it has 1 row for each employee saved to localStorage
- it has a show-pic column that starts with the text: "show pic".  If the user clicks on it, it loads the image associated with employee ID and replaces the text.

When we load
	scan data-source for employees that are missing OR have been marked terminated 
	mark them terminated
	??? display their card.
	??? log in console their info?
	??? move them to a terminated repo and out of the current repo

Add a function to cmd called showNew() that displays peopled hired since last time.

Reports
	Departments & numbers
	People in department
	People by Location sections.

Once reports are working better, remove the ids.ts file.

IVY010 / 0 ==> terminated

If lastWorkDate == "1900-01-01T00:00:00"
	then delete .lastWorkDate

Strip the time off of startDate and lastWorkedDate

Remove employeeId from from records saved to localStorage.

Export (as backup)
	- current employees
	- terminated employees
